export default function SavingsEstimatorPage() {
  return (
    <div className="flex flex-col gap-2">
      <h1 className="text-2xl font-bold">Savings estimator</h1>
      <p className="text-muted-foreground">
        Estimates the cost avoided by acting on a recommendation early —
        delay minutes saved, missed connections avoided, and the resulting
        cost impact.
      </p>
    </div>
  );
}
